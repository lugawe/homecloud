apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: opencloud
  name: opencloud-app-pvc
  labels:
    app: opencloud-app

spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
