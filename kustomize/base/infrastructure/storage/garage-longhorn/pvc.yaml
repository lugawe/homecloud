apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: storage
  name: garage-longhorn-pvc
  labels:
    app: garage-longhorn

spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
