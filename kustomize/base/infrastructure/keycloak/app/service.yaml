apiVersion: v1

kind: Service

metadata:
  namespace: keycloak
  name: keycloak
  labels:
    app: keycloak

spec:
  selector:
    app: keycloak
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: https
      port: 443
      targetPort: 8443
