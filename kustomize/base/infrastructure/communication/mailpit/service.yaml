apiVersion: v1

kind: Service

metadata:
  namespace: communication
  name: mailpit
  labels:
    app: mailpit

spec:
  selector:
    app: mailpit
  ports:
    - name: http
      port: 80
      targetPort: 8025
    - name: smtp
      port: 1025
      targetPort: 1025
