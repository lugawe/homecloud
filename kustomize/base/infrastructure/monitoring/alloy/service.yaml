apiVersion: v1

kind: Service

metadata:
  namespace: monitoring
  name: alloy
  labels:
    app: alloy

spec:
  selector:
    app: alloy
  type: ClusterIP
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: 4317
