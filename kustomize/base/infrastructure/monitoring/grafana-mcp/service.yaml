apiVersion: v1

kind: Service

metadata:
  namespace: monitoring
  name: grafana-mcp
  labels:
    app: grafana-mcp

spec:
  selector:
    app: grafana-mcp
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
