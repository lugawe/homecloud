apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: monitoring
  name: loki-pvc
  labels:
    app: loki

spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
