apiVersion: v1

kind: Service

metadata:
  namespace: monitoring
  name: mimir
  labels:
    app: mimir

spec:
  selector:
    app: mimir
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
