apiVersion: v1

kind: Service

metadata:
  namespace: monitoring
  name: grafana
  labels:
    app: grafana

spec:
  selector:
    app: grafana
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
