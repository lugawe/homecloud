apiVersion: v1

kind: Service

metadata:
  namespace: secrets-system
  name: vault
  labels:
    app: vault

spec:
  selector:
    app: vault
  type: ClusterIP
  ports:
    - name: https
      port: 8200
      targetPort: 8200
