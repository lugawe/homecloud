apiVersion: kustomize.config.k8s.io/v1beta1

kind: Kustomization

namespace: vault

resources:
  - namespace.yaml

helmCharts:
  - namespace: vault
    releaseName: vault
    repo: https://helm.releases.hashicorp.com
    name: vault
    version: 0.32.0
    includeCRDs: true
