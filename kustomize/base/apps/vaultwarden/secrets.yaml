---
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: vaultwarden
  name: vault-auth
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  namespace: vaultwarden
  name: vault-auth
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: vaultwarden
    serviceAccount: vault-auth
    audiences:
      - vault
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  namespace: vaultwarden
  name: vaultwarden-secrets
spec:
  type: kv-v2
  mount: secret
  path: homecloud/vaultwarden
  destination:
    name: vaultwarden-secrets
    create: true
    overwrite: true
    labels:
      app.kubernetes.io/part-of: vaultwarden
  vaultAuthRef: vault-auth
  refreshAfter: 1h
