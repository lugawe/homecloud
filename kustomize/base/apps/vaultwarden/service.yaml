apiVersion: v1

kind: Service

metadata:
  namespace: vaultwarden
  name: vaultwarden-app
  labels:
    app: vaultwarden-app

spec:
  selector:
    app: vaultwarden-app
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
