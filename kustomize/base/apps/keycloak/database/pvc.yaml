apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: keycloak
  name: keycloak-database-pvc
  labels:
    app: keycloak-database

spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
