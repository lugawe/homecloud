apiVersion: v1

kind: Service

metadata:
  namespace: keycloak
  name: keycloak-app
  labels:
    app: keycloak-app

spec:
  selector:
    app: keycloak-app
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
