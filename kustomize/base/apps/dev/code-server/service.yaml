apiVersion: v1

kind: Service

metadata:
  namespace: dev
  name: code-server
  labels:
    app: code-server

spec:
  selector:
    app: code-server
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
