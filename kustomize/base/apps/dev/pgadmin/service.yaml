apiVersion: v1

kind: Service

metadata:
  namespace: dev
  name: pgadmin
  labels:
    app: pgadmin

spec:
  selector:
    app: pgadmin
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
