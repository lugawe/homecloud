apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: outline
  name: outline-app-pvc
  labels:
    app: outline-app

spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
