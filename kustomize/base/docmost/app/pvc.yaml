apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: docmost
  name: docmost-app-pvc
  labels:
    app: docmost-app

spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
