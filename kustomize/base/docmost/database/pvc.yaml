apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: docmost
  name: docmost-database-pvc
  labels:
    app: docmost-database

spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
