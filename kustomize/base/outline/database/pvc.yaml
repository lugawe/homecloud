apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  namespace: outline
  name: outline-database-pvc
  labels:
    app: outline-database

spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
