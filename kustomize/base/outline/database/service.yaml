apiVersion: v1

kind: Service

metadata:
  namespace: outline
  name: outline-database-service
  labels:
    app: outline-database

spec:
  selector:
    app: outline-database
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
